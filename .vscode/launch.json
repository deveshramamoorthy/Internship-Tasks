{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (Clean Internal)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/a.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "build",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}